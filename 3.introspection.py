#!/usr/bin/env python# -*- coding: utf-8 -*-import inspectimport os.pathprint inspect.getmodule(os.path)print inspect.isclass(str)print inspect.isclass('juliana')for name, struct in inspect.getmembers(os.path):    if inspect.isfunction(struct):        print namea = 'I am a string'print id(a)print type(a)print len(a)print isinstance(a, str)print type(a) is strfor a in locals(): #tive que usar uma var que ja havia sido definida antes, senao o dic mudava de tamanho ao adicionar uma nova var    print a